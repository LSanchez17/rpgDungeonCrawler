import { Weapon } from "./Weapons";

export class Wand extends Weapon {
    constructor(name: string, price: number, attack: number, durability: number) {
        super(name, price, attack, durability);
    }

    // TODO: Implement Wand specific methods

}